services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    ports:
          - 5432:5432
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: example
      POSTGRES_DB: app_fitness

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080